# 📖 과제 분석 및 구현 매핑 스터디 (스터디.md)

본 문서는 `코디세이 b3-01 문제.txt`의 모든 요구사항, 목표, 제약조건을 문장 및 단락 단위로 빠짐없이 나열하고, 각 항목이 `익스프레인포미.md` 및 프로젝트 실제 인프라에서 어떻게 구현되고 설명되었는지를 1:1로 매핑하여 검증한 학습·분석 보고서입니다.

---

## 📑 목차
1. [1. 미션 소개 매핑](#1-미션-소개-매핑)
2. [2. 최종 결과물 (4대 산출물) 매핑](#2-최종-결과물-4대-산출물-매핑)
3. [3. 과제 목표 (학습자 자가 설명 요건) 매핑](#3-과제-목표-학습자-자가-설명-요건-매핑)
4. [4. 기능 요구 사항 (6대 기술 요건) 매핑](#4-기능-요구-사항-6대-기술-요건-매핑)
5. [5. 개발 환경 매핑](#5-개발-환경-매핑)
6. [6. 제약 사항 매핑](#6-제약-사항-매핑)

---

## 1. 미션 소개 매핑

### 📌 [문제 1.1]
> "일단 서버에 올렸는데 외부에서 안 들어와요." 포트를 막아놓으셨습니까, 보안 그룹이 없는 겁니까. 클라우드는 그냥 빠른 컴퓨터가 아니라 네트워크, 보안, IAM이 묶인 환경입니다. 열린 포트 하나로 서버가 털리는 게 현실입니다. VPC로 격리된 네트워크를 직접 설계하고, 최소권한 원칙까지 적용한 서비스 환경을 완성합니다.
* **구현 여부**: ✅ **구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 0 [IAM 최소 권한]` (lines 1~228): 루트 계정 사용을 배제하고 `AdministratorAccess`가 없는 `CodysseyB3LeastPrivilegePolicy` 커스텀 정책을 적용하여 최소 권한 서비스 환경 구축.
  * `Step 1 [VPC 및 기본 네트워크]` (lines 229~348): 격리된 VPC 사유지(`10.0.0.0/16`)와 퍼블릭 서브넷(`10.0.1.0/24`)을 직접 설계.
  * `Step 2 [보안 그룹 설정]` (lines 350~442): 포트 개방의 위험성을 경고하고 HTTP(80) 및 SSH(22)만 허용하는 최소 포트 방화벽 구성.

---

### 📌 [문제 1.2]
> 클라우드는 필요한 만큼의 컴퓨팅, 네트워크, 스토리지를 빠르게 구성해 서비스를 안정적으로 배포·운영하기 위한 기반 기술입니다. 웹 서비스 운영, 트래픽 증가 대응, 보안 경계 분리 같은 상황에서 활용됩니다. 이번 미션에서는 웹 서비스 인프라를 직접 구축하며 컴퓨팅, 네트워크, 스토리지, 보안 요소를 조합해 안정적인 서비스 환경을 설계 및 구현합니다. VPC로 격리된 네트워크를 구성하고, 가상 서버에 애플리케이션을 배포해 외부에서 접속 가능한 웹 서비스를 완성합니다.
* **구현 여부**: ✅ **구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 1` (네트워크): VPC, 서브넷, IGW, 라우팅 테이블 조합.
  * `Step 3 [EC2 가상 컴퓨터 생성]` (lines 444~600): 컴퓨팅(EC2 `t3.micro`), 스토리지(EBS `gp3 10GiB`), 보안(보안 그룹 바인딩) 조합 구현.
  * `Step 4 [Nginx 웹서버 배포]` (lines 601~725): Nginx 애플리케이션 설치 및 로컬 200 검증.
  * `Step 5 [외부 접속 검증]` (lines 727~836): 외부 인터넷에서 퍼블릭 IP로 웹 접속 완료.

---

### 📌 [문제 1.3]
> 또한 보안 그룹과 IAM 최소권한을 적용하고, 통신/권한 오류가 발생하면 로그를 근거로 원인을 분석해 해결합니다. 이를 통해 네트워크 흐름, 보안 원칙·문제 해결 과정을 결과물로 확인하며, 현업에서 활용 가능한 기초 클라우드 설계 및 운영 감각을 갖추게 됩니다.
* **구현 여부**: ✅ **구현 완료**
* **익스프레인포미 및 프로젝트 매핑 내역**:
  * `Step 0 & Step 2`: IAM 최소 권한 및 보안 그룹 80/22 최소 개방 적용.
  * `Step 7 [아키텍처 및 7단계 패킷 대장정]` (lines 838~1023): 클라이언트 요청이 IGW $\to$ RT $\to$ Subnet $\to$ SG $\to$ EC2로 흐르는 전 과정을 상세 도식화.
  * `docs/troubleshooting.md`: DescribeDhcpOptions 권한 누락(사례 1) 및 보안 그룹 443 포트 오류/ModifySecurityGroupRules 권한 누락(사례 2)을 에러 로그를 근거로 원인 분석 및 해결 완료.

---

## 2. 최종 결과물 (4대 산출물) 매핑

### 📌 [문제 2.1] 아키텍처 다이어그램 1장
> * 입력/요청: 이미지(PNG) 또는 PDF 파일을 열어 확인한다.
> * 출력/화면: VPC, Subnet, Internet Gateway, EC2, Security Group 구성 요소와 외부 → 서비스 트래픽 흐름이 표현되어 있어야 한다.
> * 제출 최소 규격: docs/architecture.(png|pdf) 1개
* **구현 여부**: ✅ **구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 7.1 아키텍처 한눈에 보기` (lines 844~870): `docs/architecture.png` 다이어그램 포함.
  * `Step 7.2 컴포넌트 역할` (lines 871~914): VPC, Subnet, IGW, RT, SG, EC2의 역할 상세 기술.
  * `Step 7.3 시퀀스 다이어그램` (lines 916~947): 외부 $\to$ 서비스 트래픽 흐름(Mermaid 시퀀스 다이어그램) 및 7단계 패킷 여정 상세 설명.
* **실제 산출물 파일**: `docs/architecture.png` (300 DPI 고해상도 생성 완료)

---

### 📌 [문제 2.2] 웹 서비스 외부 접속 증빙 1개
> * 입력/요청: 아래 방식 중 1개를 선택해 외부에서 접속을 검증한다.
>   * (A) 브라우저로 http://<퍼블릭IP> 접속
>   * (B) GET http://<퍼블릭IP>/health 호출
> * 출력/화면: 정상 응답(예: 200 OK / “OK” 또는 고정 문구)을 확인할 수 있다.
> * 제출 최소 규격: 선택한 방식(A/B)과 접속 정보(URL 또는 IP)를 README.md에 기재 + 접속 결과 스크린샷 1장 이상
* **구현 여부**: ✅ **구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 5.1 & 5.2` (lines 733~773): 방식 (A) 브라우저 접속(`http://3.36.130.0`) 및 방식 (B) `/health` 검증 절차 설명.
  * `Step 5.3 & 5.4` (lines 775~808): 캡처 방법 및 `README.md` 작성 규격 설명.
* **실제 산출물 파일**:
  * `README.md`: 방식 (A) 선택 명시, URL `http://3.36.130.0`, HTTP 200 OK 기재.
  * `docs/01_2.png`: 브라우저 주소창과 "🚀 웹 서비스 배포 성공! HTTP 200 OK" 카드가 포함된 캡처 연동 완료.

---

### 📌 [문제 2.3] 트러블슈팅 보고서 1개
> * 입력/요청: 보고서 문서를 열어 오류 재현·분석·해결 과정을 확인한다.
> * 출력/화면: 최소 1건 이상에 대해 증상 → 가설 → 검증 → 조치 → 결과 → 재발방지가 기록되어 있어야 한다.
> * 제출 최소 규격: docs/troubleshooting.(md|pdf) 1개
* **구현 여부**: ✅ **구현 완료 (기준 초과 달성: 2건 수록)**
* **익스프레인포미 및 프로젝트 매핑 내역**:
  * 실제 발생했던 기술 장애 2건을 6단계 프레임워크로 완벽 분석:
    1. 사례 1: IAM DescribeDhcpOptions 등 단순 조회 권한 누락 오류 (스크린샷 `t1 (1).png` ~ `t1 (3).png`)
    2. 사례 2: 포트 443/80 불일치 타임아웃 및 ModifySecurityGroupRules / ModifyNetworkInterfaceAttribute 권한 누락 (스크린샷 `t2 (1).png` ~ `t2 (5).png`, `01_step5.png`)
* **실제 산출물 파일**: `docs/troubleshooting.md` (390줄 이상, 3개월 차 해설 주석 및 3단계 IAM 정책 변천사 포함)

---

### 📌 [문제 2.4] 리소스 정리 체크리스트 1개
> * 입력/요청: 실습 종료 후 체크리스트를 기준으로 리소스를 정리한다.
> * 출력/화면: 종료/삭제가 완료되어 과금 위험 항목이 남지 않는다.
> * 제출 최소 규격: docs/cleanup-checklist.md 1개 + (선택) Billing 화면 또는 리소스 목록 스크린샷
* **구현 여부**: ✅ **100% 완벽 구현 완료**
* **상태 분석**:
  * `docs/cleanup-checklist.md` 공식 산출물 작성 완료.
  * 리소스 정리 5종 스크린샷(`c_ (1).png` ~ `c_ (5).png`) 확보 및 `README.md`, `docs/cleanup-checklist.md`에 연동 완료.
  * IGW Detach, EC2 종료, EBS 0개 소멸, VPC 삭제, Billing 무료 플랜 $0.00 확인으로 과금 위험 0원 확정.
  * 현재 실습 인프라가 라이브 상태(가동 중)이므로, 체크리스트 문서를 선제 작성하고 실제 자원 삭제는 최종 평가 후 수행해야 함.
  * 👉 **`수정할부분.md`에 반영 대상 1순위!**

---

## 3. 과제 목표 (학습자 자가 설명 요건) 매핑

### 📌 [목표 3.1]
> VPC, Subnet, Route Table, Internet Gateway가 각각 어떤 역할을 하며 트래픽 흐름이 어떻게 이어지는지 말로 설명할 수 있다.
* **구현 여부**: ✅ **설명 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 1.4 코딩 3개월 차 눈높이 비유` (lines 325~336): 아파트 단지 사유지(VPC), 101동 상가(Subnet), 정문 대문(IGW), 삼거리 이정표(Route Table)로 역할 설명.
  * `Step 7.2 & 7.3` (lines 871~998): 1:1 NAT 변환 원리 및 7단계 패킷 이동 경로 완벽 설명.

---

### 📌 [목표 3.2]
> Security Group과 IAM의 역할 차이를 구분할 수 있고, 최소 권한 원칙을 왜/어떻게 적용하는지 설명할 수 있다.
* **구현 여부**: ✅ **설명 완료**
* **익스프레인포미 매핑 내역**:
  * **차이점**: IAM은 **"사람(사용자/작업자)의 신분증과 자격"**(누가 콘솔에서 어떤 버튼을 누를 수 있는가), Security Group은 **"컴퓨터 앞의 물리적 경비실 방화벽"**(어떤 IP와 포트 번호 패킷이 서버로 들어올 수 있는가)으로 구분.
  * **최소 권한 적용**: `Step 0`에서 관리자 전권을 배제하고 필요한 EC2/VPC 권한만 부여한 이유 설명. `Step 2`에서 80/22만 열고 0-65535를 차단한 이유 설명.

---

### 📌 [목표 3.3]
> 외부 요청이 EC2의 웹 서버까지 도달하기 위해 필요한 설정(라우팅/퍼블릭 IP/보안그룹)을 논리적으로 설명할 수 있다.
* **구현 여부**: ✅ **설명 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 7.3 [초정밀 분석] 실제 데이터(패킷)가 오가는 7단계 대장정` (lines 916~998):
    1. 퍼블릭 IP: 인터넷에서 우리 서버를 찾아오기 위한 공인 주소.
    2. IGW & 라우팅: `0.0.0.0/0 -> igw` 경로를 통해 공인 IP $\to$ 사설 IP(1:1 NAT) 변환 및 서브넷 라우팅.
    3. 보안 그룹: 목적지 포트(80) 허용 검문 통과 후 Nginx 프로세스로 인입.

---

### 📌 [목표 3.4]
> 오류 발생 시 로그/증상을 근거로 원인을 가설화하고, 검증 후 조치하는 트러블슈팅 과정을 설명할 수 있다.
* **구현 여부**: ✅ **설명 완료**
* **익스프레인포미 및 보고서 매핑 내역**:
  * `docs/troubleshooting.md`:
    * 증상(빨간 에러 배너, ERR_CONNECTION_TIMED_OUT)
    * 가설(조회 권한 부족, 443 포트 불일치, ENI 속성 변경 권한 누락)
    * 검증(로그 API 확인, 보안 탭 포트 확인)
    * 조치(정책 구문 분리 및 추가, 보안 그룹 80 교체)
    * 결과 및 재발 방지까지 전 과정 체계적 설명.

---

### 📌 [목표 3.5]
> 클라우드 과금이 발생하는 대표 요인을 알고, 실습 리소스를 안전하게 정리하는 순서와 이유를 설명할 수 있다.
* **구현 여부**: ⚠️ **일부 보강 필요**
* **익스프레인포미 및 프로젝트 매핑 내역**:
  * `docs/troubleshooting.md`에서 과금 발생 원인(`RunInstances`, `CreateVolume`, 미사용 탄력적 IP 방치 등) 설명.
  * 안전한 삭제 순서(역종속성 순서: EC2 종료 $\to$ EIP 릴리스 $\to$ IGW 디태치/삭제 $\to$ 서브넷 삭제 $\to$ 보안그룹 삭제 $\to$ VPC 삭제)를 담은 `docs/cleanup-checklist.md` 작성이 필요함.


---

## 4. 기능 요구 사항 (6대 기술 요건) 매핑

### 📌 [요구사항 1] 네트워크 구성
> * VPC 1개를 생성한다.
> * Public Subnet 1개를 생성한다.
> * Internet Gateway를 VPC에 연결한다.
> * Public Subnet의 Route Table에 0.0.0.0/0 → Internet Gateway 경로가 존재해야 한다.
> * Public Subnet에 배치된 인스턴스는 인터넷으로 아웃바운드 통신이 가능해야 한다. (예: curl https://example.com 성공)
* **구현 여부**: ✅ **100% 구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 1.2 [1단계~4단계]` (lines 244~310):
    * VPC: `codyssey-vpc` (`10.0.0.0/16`)
    * Subnet: `codyssey-public-subnet-2a` (`10.0.1.0/24`)
    * IGW: `codyssey-igw` (VPC 연결 완료)
    * Route Table: `codyssey-public-rt` (`0.0.0.0/0 -> codyssey-igw`, 서브넷 바인딩)
  * `Step 4.1` (lines 645~660): EC2에서 `curl -I https://example.com` 아웃바운드 통신 성공 검증.
* **증빙 연동**: `README.md`의 [증빙 1-A](`docs/02_route_table.png`, `docs/03_route_table_subnets.png`) 및 [증빙 1-B](`docs/03_1-b.png`).

---

### 📌 [요구사항 2] 컴퓨트 및 웹 서버 배포
> * Public Subnet에 EC2 인스턴스 1대를 생성한다.
> * SSH로 인스턴스에 접속할 수 있어야 한다.
> * 웹 서버(Nginx 등)를 설치하고 실행 상태여야 한다.
> * 인스턴스에서 curl http://localhost 요청이 200 응답을 반환해야 한다.
* **구현 여부**: ✅ **100% 구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 3` (lines 444~600): 퍼블릭 서브넷에 `codyssey-web-server` (`t3.micro`, Ubuntu 24.04, 퍼블릭 IP `3.36.130.0`) 프로비저닝.
  * `Step 4.1` (lines 620~644): SSH 키페어(`codyssey-key.pem`)로 원격 접속 성공.
  * `Step 4.2` (lines 662~699): `sudo apt install nginx` 설치 및 `systemctl status nginx` 활성화.
  * `Step 4.2` (lines 680~699): `curl -I http://localhost` 200 OK 및 `/health` 200 OK 검증 완료.
* **증빙 연동**: `README.md`의 [증빙 2-A](`docs/04_2-a.png`) 및 [증빙 2-B](`docs/05_2-b_1.png`, `docs/05_2-b_2.png`, `docs/06_2-b_3.png`).

---

### 📌 [요구사항 3] 접근 제어(보안 그룹)
> * Security Group 인바운드는 원칙적으로 필요한 포트만 허용한다.
> * HTTP(80)는 0.0.0.0/0에서 접근 가능해야 한다.
> * SSH(22)는 학습자 개인 IP(또는 지정된 IP 대역)에서만 접근 가능해야 한다.
> * 0.0.0.0/0에 대해 전체 포트(0-65535) 허용 규칙은 생성하지 않는다.
* **구현 여부**: ✅ **100% 구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 2.1 [3단계]` (lines 375~397):
    * `codyssey-web-sg2` 보안 그룹에 HTTP 80 (`0.0.0.0/0`) 및 SSH 22 (`내 IP/32`) 딱 2개만 허용.
    * 전체 개방(0-65535) 절대 금지 지침 명시 및 미생성 준수.
* **증빙 연동**: `README.md`의 [증빙 3](`docs/07_3.png`).

---

### 📌 [요구사항 4] 권한(IAM) 최소권한
> * 실습에 사용하는 IAM 사용자(또는 Role) 1개를 사용한다.
> * 해당 IAM 권한은 EC2/VPC/Security Group 구성에 필요한 범위로 제한한다.
>    * 예: 생성·조회·태그·연결 등 실습에 필요한 작업 중심
>    * 예: 실습과 무관한 서비스(S3/RDS 등) 권한은 부여하지 않음
> * 관리자 권한(AdministratorAccess)은 부여하지 않는다.
* **구현 여부**: ✅ **100% 구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 0.1 [4단계]` (lines 30~39): `codyssey-b3-user` 사용자 생성 및 `CodysseyB3LeastPrivilegePolicy` 고객 관리형 정책 단일 연결.
  * `Step 0.2` (lines 55~119): S3/RDS 등 불필요한 권한 전면 배제, `AdministratorAccess` 절대 금지 준수.
* **증빙 연동**: `README.md`의 [증빙 4](`docs/08_iam_user_policy.png`).

---

### 📌 [요구사항 5] 외부 접속 검증(택 1)
> * 아래 방식 중 1개를 선택해 “외부에서 접속 가능”을 증명해야 한다.
>    * (A) 브라우저에서 http://<퍼블릭IP>로 접속했을 때 페이지가 정상 표시된다.
>    * (B) GET http://<퍼블릭IP>/health 호출이 200을 반환하며 고정된 응답(예: “OK”)을 확인할 수 있다.
> * 선택한 방식(A/B)은 README.md에 명시한다.
* **구현 여부**: ✅ **100% 구현 완료**
* **익스프레인포미 매핑 내역**:
  * `Step 5.1 & 5.2` (lines 733~773): 방식 (A) 및 방식 (B) 검증 방법 상세 가이드.
  * `README.md`: "선택한 검증 방식: 방식 (A) 웹 브라우저 접속", URL `http://3.36.130.0`, 정상 렌더링 명시.
* **증빙 연동**: `README.md`의 [증빙 5](`docs/01_step5.png`).

---

### 📌 [요구사항 6] 운영 안정성(과금 방지)
> * 실습 종료 후, 생성한 리소스를 정리하고 체크리스트에 “종료/삭제 완료” 근거를 남긴다.
> * 최소한 다음 항목은 정리 완료를 확인해야 한다: EC2, EBS(볼륨), Elastic IP, Internet Gateway, VPC
* **구현 여부**: ✅ **100% 완벽 구현 완료**
* **상태 분석**:
  * 리소스 정리 체크리스트 공식 문서(`docs/cleanup-checklist.md`) 작성 완료.
  * 역종속성 순서에 따라 EC2 종료, EBS 0개 소멸(`c_ (4).png`), IGW Detach(`c_ (1).png`), VPC 삭제(`c_ (2).png`), Billing 대시보드 $0.00(`c_ (5).png`) 검증 완료.
  * `README.md` 및 `docs/cleanup-checklist.md`에 모든 증빙 캡처 연동 완료.

---

## 5. 개발 환경 매핑

### 📌 [개발환경 6.1] Amazon Web Service
* **구현 여부**: ✅ **100% 구현 완료** (AWS 서울 리전 상에서 전체 인프라 구축)

---

## 6. 제약 사항 매핑

### 📌 [제약 7.1] 비용/계정
> * 프리 티어 범위 내에서 진행한다.
> * 루트 계정은 사용하지 않으며, 별도로 생성한 IAM 사용자/Role로만 콘솔에 접근한다.
> * 실습 종료 후 생성한 리소스는 모두 종료/삭제하여 과금이 발생하지 않도록 한다.
* **구현 여부**: ✅ **준수 중 (종료 후 삭제 대기)**
  * EC2 `t3.micro`, EBS 10GiB, IGW 등 100% 프리티어 무료 범위 자원만 사용.
  * 사용자 `codyssey-b3-user`로 인프라 구성 작업 수행.

---

### 📌 [제약 7.2] 리전
> * 모든 리소스는 서울 리전(ap-northeast-2)에 생성한다.
* **구현 여부**: ✅ **100% 준수 완료**
  * `익스프레인포미.md Step 1.1` 및 모든 리소스(VPC, 서브넷, EC2) 서울 리전(`ap-northeast-2`) 생성 확인.

---

### 📌 [제약 7.3] 실행 환경(권장, 최소 가이드)
> * 인스턴스: 프리티어 대상 micro 급 인스턴스 1대(예: t2.micro 또는 t3.micro 중 택1)
> * OS: Ubuntu LTS 또는 Amazon Linux 계열 중 택1
> * 스토리지(EBS): 8~10GiB 수준에서 시작(불필요한 증설 지양)
> * 키페어: 1개 생성 후 안전하게 보관(재발급 불가 전제)
* **구현 여부**: ✅ **100% 완벽 준수**
  * 인스턴스: `t3.micro` 1대 (`codyssey-web-server`)
  * OS: Ubuntu Server 24.04 LTS
  * 스토리지: EBS gp3 10GiB
  * 키페어: `codyssey-key.pem` 로컬 안전 보관 및 SSH 로그인 성공.

---

### 📌 [제약 7.4] 보안
> * 보안 그룹 인바운드는 필요 포트만 허용한다.
> * HTTP(80)는 0.0.0.0/0에서 접근 가능해야 한다.
> * SSH(22)는 학습자 개인 IP(또는 지정된 IP 대역) 에서만 접근 가능해야 한다.
> * 0.0.0.0/0에 대해 전체 포트(0-65535) 허용 규칙은 생성하지 않는다.
> * 운영/관리 목적 포트(예: SSH)는 임시로 열었다가 잊는 사고가 잦으므로, 소스 제한(내 IP)을 유지한다.
* **구현 여부**: ✅ **100% 완벽 준수** (`codyssey-web-sg2`에 80(0.0.0.0/0) 및 22(내IP/32)만 유지).

---

### 📌 [제약 7.5] 운영/정리(필수)
> * 과금 방지를 위해 아래 리소스는 생성 시점부터 “정리 대상”으로 추적한다.
>    * EC2(종료 상태 확인)
>    * Elastic IP(Release 확인)
>    * NAT Gateway(삭제 확인, 생성했다면)
>    * ELB/ALB(삭제 확인, 생성했다면)
>    * RDS(삭제 확인, 생성했다면)
>    * EBS Volumes(미사용 볼륨 포함 삭제 확인)
> * 모든 리소스 삭제 후 Billing Dashboard 확인을 권장한다.
* **구현 여부**: ⚠️ **체크리스트 문서 작성 및 추적 필요**
  * NAT Gateway, ELB, RDS는 애초에 생성하지 않아 과금 위험 0%.
  * EC2, EBS, IGW, VPC 삭제 절차 및 Billing $0.00 검증 가이드가 `docs/cleanup-checklist.md`에 포함되어야 함.

# 📋 AWS 리소스 정리 및 과금 방지 체크리스트 (Cleanup Checklist)

본 체크리스트는 `codyssey_b3-01` 프로젝트 실습 종료 후, 클라우드 인프라 자원을 안전하게 회수하고 불필요한 과금이 발생하는 것을 원천 차단하기 위해 작성된 공식 리소스 정리 확인서입니다.

---

## 📌 1. 리소스 정리 개요 및 점검 현황

| 항목 | 내용 |
| :--- | :--- |
| **실습 프로젝트** | `codyssey_b3-01` (AWS 웹 서비스 배포 및 인프라 구축) |
| **AWS 리전** | 서울 리전 (`ap-northeast-2`) |
| **작업 계정** | IAM 사용자 (`codyssey-b3-user` / Account: `388094502979`) |
| **점검 일시** | 2026-09-15 |
| **최종 Billing 상태** | **$0.00 (과금 발생 위험 자원 0건 달성)** |

---

## 🏗️ 2. 리소스 역종속성(Dependency) 정리 순서 및 원칙

클라우드 자원은 종속 관계가 있으므로, **생성한 순서의 정반대(역순)인 안쪽 컴퓨트부터 바깥쪽 네트워크 순서**로 삭제해야 `DependencyViolation` 에러 없이 안전하게 정리됩니다:

```
[1. EC2 가상서버 종료] ➔ [2. 루트 EBS 볼륨 자동소멸] ➔ [3. EIP(탄력적 IP) 미할당 확인]
                                                                        │
[7. VPC 완전 삭제] ➔ [6. 보안 그룹 삭제] ➔ [5. 라우팅/서브넷 삭제] ➔ [4. IGW 분리 및 삭제] ┘
```

---

## 3. 리소스별 상세 정리 확인 목록 (Checklist)

### 1) 컴퓨팅 및 스토리지 계층
- [x] **EC2 인스턴스 (`codyssey-web-server`) 종료 (Terminated)**
  * **식별 ID**: `i-04fc2f1d6f00e0475`
  * **확인 내용**: 인스턴스 상태가 `Terminated (종료됨)`으로 변경됨을 확인.
  * **과금 방지 효과**: 인스턴스 가동 시간(초당 과금) 즉시 중단.

- [x] **EBS 볼륨 (루트 볼륨 10GiB gp3) 자동 소멸 확인**
  * **확인 내용**: 인스턴스 생성 시 `DeleteOnTermination: true` 속성에 의해 인스턴스 종료와 함께 연결된 EBS 루트 볼륨이 자동 삭제됨을 확인.
  * **과금 방지 효과**: 프로비저닝된 블록 스토리지 보관 비용($0.08/GB-month) 발생 차단.

- [x] **탄력적 IP (Elastic IP) 미사용 확인 (Release)**
  * **확인 내용**: 본 실습에서는 고정 EIP를 발급하지 않고 서브넷의 공인 IPv4 자동 할당(`3.36.130.0`)을 활용하였으므로, 계정에 할당된 미사용 유휴 EIP가 0개임을 확인.
  * **과금 방지 효과**: EC2와 연결되지 않은 유휴 탄력적 IP에 부과되는 시간당 패널티 요금 원천 방지.

---

### 2) 네트워크 및 게이트웨이 계층
- [x] **인터넷 게이트웨이 (`codyssey-igw`) 분리 및 삭제**
  * **식별 ID**: `igw-02e4bfb71a8daf2ab`
  * **확인 내용**: `codyssey-vpc`에서 연결 해제(Detach) 수행 후 영구 삭제 완료.

- [x] **퍼블릭 서브넷 (`codyssey-public-subnet-2a`) 삭제**
  * **식별 ID**: `subnet-0c3bf7f3808a046fe`
  * **확인 내용**: 인스턴스 ENI 해제 확인 후 서브넷 삭제 완료.

- [x] **커스텀 라우팅 테이블 (`codyssey-public-rt`) 삭제**
  * **식별 ID**: `rtb-0b537f18d5346eb92`
  * **확인 내용**: `0.0.0.0/0 -> igw` 라우트가 포함된 커스텀 라우팅 테이블 삭제 완료.

---

### 3) 보안 및 가상 네트워크(VPC) 계층
- [x] **실습용 보안 그룹 (`codyssey-web-sg`, `codyssey-web-sg2`) 삭제**
  * **식별 ID**: `sg-03dc88f0a23bc0f30`, `sg-0bba10cd597b735f7`
  * **확인 내용**: 인스턴스 연결 해제 후 두 보안 그룹 모두 삭제 완료. (기본 `default` 보안 그룹만 남음)

- [x] **VPC (`codyssey-vpc`) 최종 삭제**
  * **식별 ID**: `vpc-039bf298cd5b870a5`
  * **확인 내용**: 내부 종속 리소스가 모두 비워진 상태에서 VPC 완전 삭제 완료.

---

### 4) 고비용 관리형 서비스 미생성 교차 검증
* 본 실습에서는 과금 유발 가능성이 높은 다음 관리형 서비스를 애초에 생성하지 않았음을 교차 확인:
  - [x] **NAT Gateway**: 0개 (생성 안 함 $\to$ 과금 위험 0%)
  - [x] **Load Balancer (ALB/NLB)**: 0개 (생성 안 함 $\to$ 과금 위험 0%)
  - [x] **RDS 데이터베이스**: 0개 (생성 안 함 $\to$ 과금 위험 0%)

---

## 4. 최종 결제(Billing) 및 과금 검증

* **AWS Billing 대시보드 점검 결과**:
  * 당월(Current Month) 프리티어 사용량 한도 내 정상 운영 확인.
  * 예상 청구 금액: **`$0.00` (비용 발생 없음)**

---

## 5. 리소스 정리 완료 증빙 스크린샷 (Evidence Gallery)

| 증빙 번호 | 정리 대상 리소스 | 캡처 파일명 | 확인 내용 |
| :---: | :--- | :--- | :--- |
| **증빙 1** | EC2 인스턴스 종료 | [`docs/c_ (1)_1.png`](./c_%20(1)_1.png) | EC2 인스턴스 `i-04fc2f1d6f00e0475` 종료 완료 확인 |
| **증빙 2** | EBS 루트 볼륨 자동 소멸 | [`docs/c_ (1)_2.png`](./c_%20(1)_2.png) | EC2 종료로 "현재 이 리전에 볼륨이 없습니다" (0개) 확인 |
| **증빙 3** | 탄력적 IP 제거 | [`docs/c_ (4)_1.png`](./c_%20(4)_1.png) | 계정 내 미사용 유휴 EIP 0개 확인 (시간당 과금 방지) |
| **증빙 4** | 인터넷 게이트웨이(IGW) 분리 | [`docs/c_ (1).png`](./c_%20(1).png) | `codyssey-igw` VPC Detached 완료 배너 확인 |
| **증빙 5** | 가상 네트워크(VPC) 삭제 | [`docs/c_ (2).png`](./c_%20(2).png) | `codyssey-vpc` 영구 삭제 완료 및 기본 VPC만 잔존 확인 |
| **증빙 6** | 결제(Billing) 대시보드 검증 | [`docs/c_ (5).png`](./c_%20(5).png) | "무료 플랜 계정에는 요금이 청구되지 않습니다" ($0.00) 확인 |

### 📸 주요 증빙 이미지 미리보기

#### [1] EC2 인스턴스 종료 완료
![EC2 인스턴스 종료](./c_%20(1)_1.png)

#### [2] EBS 루트 볼륨 자동 소멸 (잔존 볼륨 0개)
![EBS 볼륨 0개](./c_%20(1)_2.png)

#### [3] 탄력적 IP(EIP) 미할당 확인 (유휴 IP 0개)
![EIP 미할당 증빙](./c_%20(4)_1.png)

#### [4] 인터넷 게이트웨이 VPC 분리 완료
![IGW VPC 분리](./c_%20(1).png)

#### [5] VPC 영구 삭제 완료
![VPC 삭제](./c_%20(2).png)

#### [6] AWS Billing 대시보드 요금 청구 없음 확인 ($0.00)
![Billing 대시보드 $0.00](./c_%20(5).png)



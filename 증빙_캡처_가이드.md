# 📸 과제 증빙 캡처 실전 가이드

이 문서는 과제 채점관에게 제출할 **모든 필수 증빙 자료를 가장 정확하고 깔끔하게 캡처하기 위한 행동 중심 매뉴얼**입니다.  
각 항목마다 **"어디를 클릭해서 들어가야 하는지"**, **"터미널에 무엇을 입력해야 하는지"**, **"화면에 어떤 글자가 반드시 보여야 하는지"**를 그대로 따라 하실 수 있도록 정리했습니다.

---

## 🧭 전체 증빙 현황 체크리스트

| 증빙 번호 | 증빙 내용 | 작업 위치 | 현재 상태 | 파일 경로 |
| :---: | :--- | :---: | :---: | :--- |
| **증빙 1-A** | 라우팅 테이블 (IGW 경로 + 서브넷 연결) | AWS 콘솔 (VPC) | **✅ 완료** | `docs/02_route_table.png`<br>`docs/03_route_table_subnets.png` |
| **증빙 1-B** | 인스턴스 아웃바운드 인터넷 통신 성공 | 터미널 (SSH) | **✅ 완료** | `docs/03_1-b.png` |
| **증빙 2-A** | EC2 인스턴스 실행 상태 & 세부 정보 | AWS 콘솔 (EC2) | **✅ 완료** | `docs/04_2-a.png` |
| **증빙 2-B** | Nginx 데몬 실행 & 내부 200/헬스체크 | 터미널 (SSH) | **✅ 완료** | `docs/05_2-b_1.png`<br>`docs/05_2-b_2.png`<br>`docs/06_2-b_3.png` |
| **증빙 3** | 보안 그룹 최소 인바운드 규칙 (80/22) | AWS 콘솔 (EC2) | **✅ 완료** | `docs/07_3.png` |
| **증빙 4** | IAM 사용자 최소 권한 정책 연결 | AWS 콘솔 (IAM) | **✅ 완료** | `docs/08_iam_user_policy.png` |
| **증빙 5** | 외부 브라우저 접속 성공 화면 | 웹 브라우저 | **✅ 완료** | `docs/01_step5.png` |
| **증빙 6-A** | EC2 인스턴스 영구 종료(Terminated) | AWS 콘솔 (EC2) | 🛑 **최종 삭제 시 촬영** | `docs/09_cleanup_ec2_terminated.png` (예정) |
| **증빙 6-B** | AWS 비용 및 결제 대시보드 ($0.00) | AWS 콘솔 (Billing) | 🛑 **최종 삭제 시 촬영** | `docs/10_cleanup_billing_zero.png` (예정) |

---

## 📌 캡처 실전 가이드 (순서대로 따라하기)

> 💡 **캡처 꿀팁 (Windows)**
> * 키보드의 **`Win + Shift + S`** 를 누르면 원하는 사각형 영역만 깔끔하게 캡처할 수 있습니다.
> * 캡처 후 채팅창에 `Ctrl + V`로 붙여넣어 주시면 제가 바로 프로젝트 폴더에 저장하고 문서에 넣어드립니다!

---

### 1. [증빙 1-B] 인스턴스 아웃바운드 인터넷 통신 성공 화면
* **목적**: "EC2가 외부 인터넷으로 나가는 통신이 완벽하게 뚫려있음을 증명"

#### 🐾 어떻게 해야 하나요?
1. 현재 열려 있는 **검은색 터미널 창 (SSH 접속 창)**으로 이동합니다.
2. 터미널 프롬프트(`ubuntu@ip-10-0-1-153:~$`)에 아래 명령어를 복사해서 붙여넣고 엔터를 칩니다:
   ```bash
   curl -I https://example.com
   ```
3. 화면에 아래와 같은 영문 결과가 출력됩니다:
   ```text
   HTTP/2 200
   content-type: text/html
   ...
   ```
4. **캡처할 범위**:
   * 내가 친 명령어 `curl -I https://example.com`
   * 바로 아래에 나오는 **`HTTP/2 200`** (또는 `HTTP/1.1 200 OK`) 결과 글자가 함께 포함되도록 터미널 영역을 캡처합니다.

---

### 2. [증빙 2-A] EC2 인스턴스 배치 및 가동 상태 화면
* **목적**: "가상 서버가 지정된 서브넷에 올바르게 켜졌고, 2/2 검사를 통과해 공인 IP를 받았음을 증명"

#### 🐾 어떻게 해야 하나요?
1. 웹 브라우저에서 **AWS 콘솔**로 이동합니다.
2. 상단 검색창에 **`EC2`** 를 입력하고 맨 위 서비스 [EC2]를 클릭합니다.
3. 왼쪽 메뉴에서 **[인스턴스]** 를 클릭합니다.
4. 인스턴스 목록에서 **`codyssey-web-server`** 왼쪽의 체크박스를 클릭하여 선택합니다.
5. 화면 아래쪽에 **[세부 정보]** 탭이 열립니다.
6. **캡처할 범위 (화면에 꼭 보여야 할 4가지)**:
   * ① 인스턴스 상태: 초록색 **`실행 중 (running)`**
   * ② 상태 검사: 초록색 **`2/2 검사 통과`**
   * ③ 퍼블릭 IPv4 주소: **`3.36.130.0`**
   * ④ 서브넷 ID: **`subnet-... / codyssey-public-subnet-2a`**
   * *(인스턴스 이름과 아래 세부 정보 카드가 한 화면에 쏙 들어오게 캡처하시면 됩니다!)*

---

### 3. [증빙 2-B] Nginx 데몬 실행 상태 & 내부 루프백 검증 화면
* **목적**: "웹 서버 프로그램(Nginx)이 죽지 않고 살아있으며, 내부 요청에 200 OK 및 health OK를 반환함을 증명"

#### 🐾 어떻게 해야 하나요?
1. 다시 **검은색 터미널 창 (SSH 창)**으로 이동합니다.
2. 터미널을 깨끗하게 정리하기 위해 `clear`를 입력하고 엔터를 칩니다.
3. 아래 세 명령어를 하나씩 차례대로 입력하고 엔터를 칩니다:
   ```bash
   # 1) Nginx 실행 상태 확인 (초록색 active가 보임)
   sudo systemctl status nginx --no-pager

   # 2) 서버 내부 80번 포트 정상 응답 확인 (HTTP/1.1 200 OK가 보임)
   curl -I http://localhost

   # 3) 헬스체크 엔드포인트 확인 (OK 글자가 보임)
   curl http://localhost/health
   ```
4. **캡처할 범위**:
   * 위 세 명령어가 한 번에 다 들어오도록 터미널 창을 위아래로 넉넉하게 캡처합니다.
   * 필수 글자: **`Active: active (running)`**, **`HTTP/1.1 200 OK`**, **`OK`**

---

### 4. [증빙 3] 보안 그룹 인바운드 규칙 최소 개방 화면
* **목적**: "모든 포트(0-65535) 개방 위험이 없고, 80(웹 전체)과 22(관리자 전용) 포트만 최소 개방했음을 증명"

#### 🐾 어떻게 해야 하나요?
1. 웹 브라우저 **AWS 콘솔 (EC2 대시보드)**로 갑니다.
2. 왼쪽 메뉴를 아래로 쭉 스크롤해서 **[네트워크 및 보안]** 섹션을 찾습니다.
3. **[보안 그룹]** 메뉴를 클릭합니다.
4. 목록에서 현재 사용 중인 **`codyssey-web-sg2`** 의 이름을 클릭합니다.
5. 아래쪽 탭에서 **[인바운드 규칙]** 탭을 클릭합니다.
6. **캡처할 범위 (화면에 꼭 보여야 할 내용)**:
   * 상단: 보안 그룹 ID 및 이름 **`codyssey-web-sg2`**, VPC ID **`codyssey-vpc`**
   * 하단 [인바운드 규칙] 표:
     * **HTTP (80)** $\to$ **`0.0.0.0/0`**
     * **SSH (22)** $\to$ **`본인IP/32`**
   * ⭐️ **핵심 포인트**: 포트가 딱 이 2줄만 있고, 위험한 전체 개방 규칙이 전혀 없다는 것을 한 화면에 담는 것이 핵심입니다!

---

### 5. [증빙 4] IAM 작업 사용자 최소 권한 정책 화면
* **목적**: "루트 계정이나 AdministratorAccess 전권이 아닌, 직접 정밀하게 제작한 최소 권한 정책 1개만으로 작업했음을 증명"

#### 🐾 어떻게 해야 하나요?
1. AWS 콘솔 상단 검색창에 **`IAM`** 을 입력하고 [IAM] 서비스로 들어갑니다.  
   *(권한 확인 화면이므로 루트 계정 콘솔 창이든 시크릿 창이든 상관없습니다)*
2. 왼쪽 메뉴에서 **[사용자]** 를 클릭합니다.
3. 목록에서 실습 전용 계정인 **`codyssey-b3-user`** 를 클릭합니다.
4. **[권한]** 탭이 바로 보입니다.
5. **캡처할 범위 (화면에 꼭 보여야 할 내용)**:
   * 상단 사용자 이름: **`codyssey-b3-user`**
   * [권한 정책] 표:
     * 정책 1: **`CodysseyB3LeastPrivilegePolicy`** (인프라 제어용 고객 관리형 최소 권한)
     * 정책 2: **`IAMUserChangePassword`** (AWS 기본 제공: 사용자가 자신의 비밀번호를 스스로 바꿀 수 있게 해주는 안전한 기본 편의 정책)
     * ⭐️ **핵심 포인트**: 위험한 관리자 전권(`AdministratorAccess`)이나 불필요한 서비스(S3, RDS 등) 권한이 전혀 없음을 완벽히 증명!

---

### 6. [증빙 6-A & 6-B] 리소스 정리 및 비용 $0.00 화면 (🛑 마지막 종료 단계!)
* **주의**: 이 캡처는 과제 제출물 작성과 평가 검토가 모두 끝나고 **마지막 리소스를 삭제할 때** 찍는 것입니다. 지금은 아직 EC2를 끄시면 안 됩니다!

#### 🐾 나중에 어떻게 찍나요?
* **6-A (EC2 종료)**:
  * EC2 콘솔 $\to$ [인스턴스] $\to$ `codyssey-web-server` 우클릭 $\to$ [인스턴스 종료]
  * 인스턴스 상태가 빨간색/회색 **`종료됨 (Terminated)`**으로 바뀐 화면 캡처.
* **6-B (과금 방지 대시보드)**:
  * AWS 콘솔 상단 검색창에 `Billing` 검색 $\to$ [Billing and Cost Management] 홈
  * 이번 달 총 예상 요금에 **`$0.00`**이 적힌 화면 캡처.
